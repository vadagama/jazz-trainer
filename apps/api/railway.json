{
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd ../.. && npm install --no-package-lock && npm run build -w @jazz/api"
  },
  "deploy": {
    "startCommand": "cd ../.. && node apps/api/dist/index.js",
    "healthcheckPath": "/api/health",
    "healthcheckTimeout": 10
  }
}
