# Iteration 002 — Prompt

- Timestamp: 2026-07-04 01:35 GMT+5
- Mode: architecture
- Type: answers to iteration 001 open questions + refinements

## Raw prompt (ответы на open questions 001)

1) Это вообще не требуется. Если потребуется глушить грув, то я на этих ячейках просто
   не буду размещать молекулы. → mode replace НЕ нужен.
2) Пусть будет только уровень cell.velocity, которая будет влиять на velocity отдельных
   молекул. → единственный velocity-контрол = cell.velocity; volumeMul и per-clip velocityMul убрать.
3) Сделаем так — что для каждого пула может быть коллекция из молекул с весами, и они
   будут произвольным образом применяться в заданном отрезке клетки. → клип несёт
   взвешенный ПУЛ молекул, применяемый случайно (по весам) на своём отрезке.
4) Организмы останутся. Они будут содержать последовательность из клеток. → организм =
   последовательность клеток (без изменений концепта).
5) Мигрируем сразу. → все 20 клеток конвертируем сразу.
6) Пересечение клипов запрещаем. И в рамках клетки не может быть двух одинаковых молекул,
   которые дублируются — ни в клетке, ни в лейне клетки. → клипы лейна не пересекаются;
   moleculeId уникален в пределах всей клетки.
7) velocity-envelope: нужно несколько типов + уровень изменения velocity. → dynamics =
   { type: несколько вариантов, amount: глубина }.
8) Для каждого лейна должна быть probability для «иногда» сбивок. → probability на уровне лейна.
9) Двухуровневый zoom (таймлайн клетки → таблица молекулы) — НЕ нужно делать.
