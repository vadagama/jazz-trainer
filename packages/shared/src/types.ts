/**
 * DTO types shared between web and api.
 *
 * NOTE (F0): stubs to be filled in by later features —
 *   - GridContent / Bar / ChordSlot → F1 (DSL parser)
 *   - UserDTO / UserSettingsDTO / HarmonyGridDTO → F4/F5 (auth + grids API)
 *   - PatternInfoDTO / GenerateInput → F3/F5 (generator)
 *
 * They live here so both apps can import a single source of truth.
 */

export {};
