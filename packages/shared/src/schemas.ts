/**
 * Zod schemas — the single source of truth for validation (api input, web forms).
 *
 * NOTE (F0): stubs to be filled in by later features —
 *   - GridContentSchema → F1 (DSL parser / data model)
 *   - GenerateSchema → F3/F5 (generator endpoint)
 *   - settings / auth schemas → F4/F5
 */

export {};
