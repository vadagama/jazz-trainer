/**
 * Rhodes instrument manifest — re-exported from @jazz/music-core.
 *
 * The engine (`RhodesInstrument`), pattern-engine molecules/cells/organisms
 * and the voicing builders all live in music-core (pure, no browser APIs),
 * following the INSTRUMENT-PLUGIN.md target architecture: "the engine lives
 * in core, the plugin supplies only manifest + sample routing."
 *
 * See docs/RHODES.md and docs/MELODIC-PLUGIN.md §11.
 */
export { rhodesManifest } from '@jazz/music-core';
