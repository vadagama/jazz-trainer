# SOPS configuration for Amazilia
# 
# Генерация ключа:
#   brew install sops age
#   age-keygen -o ~/.config/sops/age/keys.txt
#
# Публичный ключ будет выведен при генерации — вставь его ниже.

creation_rules:
  - path_regex: .*\.encrypted\.(yaml|json|env)$
    age: <публичный-age-ключ>
